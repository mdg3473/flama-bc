export const Hero = () => {
  return (
    <section id="top" className="relative h-[85vh] w-full overflow-hidden">
      {/* Transparent — shows only the fixed page background/video */}
    </section>
  );
};
